###########################################
######Redis
###########################################
spring.redis.host=localhost
spring.redis.port=6379
spring.redis.pool.max-idle=8
spring.redis.pool.min-idle=0
spring.redis.pool.max-active=8
spring.redis.pool.max-wait=-1


########################################################
###datasource
########################################################
spring.datasource.url = jdbc:mysql://mysql_yxy_course_rw.dev.office:3306/test10?characterEncoding=UTF-8
spring.datasource.username = root
spring.datasource.password = root
spring.datasource.driverClassName = com.mysql.jdbc.Driver
spring.datasource.max-active=20
spring.datasource.max-idle=8
spring.datasource.min-idle=8
spring.datasource.initial-size=10

mybatis.mapperLocations=classpath:mapper/*.xml
mybatis.configuration.cache-enabled=true

logging.level.com.xf.zhang.mapper=DEBUG

#logging settings
logging.level.org.springframework.web=INFO
logging.file=${user.home}/logs/springboot-redis.log
logging.pattern.console= %d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36}:%L - %msg%n
logging.pattern.file= %d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36}:%L - %msg%n

project.name=Springboot-Redis Application

server.port=8080

# 控制台地址
spring.cloud.sentinel.transport.dashboard=localhost:8080 

